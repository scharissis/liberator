var express = require('express');
var path = require('path');
var cookieParser = require('cookie-parser');
var bodyParser = require('body-parser');
var swagger = require("swagger-n");
var request = require('request');

//var spec = require('./spec');
//var handlers = require('./handlers');

var app;

before(function() {
	app = express();

	app.use(bodyParser.json());
	app.use(bodyParser.urlencoded({ extended: false }));
	app.use(cookieParser());

//	app.use(swagger.router(spec, handlers));

//	app.listen(8000);
});

after(function () {
	app.close();
});

//describe('app', function() {
//
//	it('/swagger.json should return 200', function (done) {
//		request.get('http://localhost:8000/swagger.json', function (err, res, body){
//			expect(res.statusCode).to.equal(200);
//			expect(res.body).to.equal(spec);
//			done();
//		});
//	});
//
//});